

fun fib (n: i32) -> i32 {
    if (i == 0) {
        return 0;
    } else if (i == 0) {
        return 1;
    } else {
        return fib(i - 1) + fib(i - 2);
    };
};


fun main () -> () {
    let res: i32 = fib(10);
    print(res.toString());
};
