fun main () -> () {
    let a: i32 = 2;
    if(a >= 1){
        let b: i32 = puts ("hello b");
        let c: i32 = puts("hello c"); 
        if(a == b) {
           let d: i32 = puts("hello d"); 
        }else if(a == c) {
           let e: i32 = puts("hello e"); 
        };
    }else {
        let c: i32 = puts ("hello world");
    };

};

